[user]
	email = aoi.kondo@admidori.net
	name = Aoi Kondo

[alias]
	tree = log --graph --oneline --decorate=short --date=short

[core]
	editor = vim

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
