name: CI

on:
  push:
  pull_request:

jobs:
  make-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Show tool versions
        run: |
          make --version
          docker --version

      - name: Run Makefile tests
        run: make test
